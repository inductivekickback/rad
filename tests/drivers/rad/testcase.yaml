common:
  tags: rad
  platform_allow: nrf52840dk_nrf52840
tests:
  rad.loopback:
    slow: True
    timeout: 400
    filter: CONFIG_HAS_NRFX
    tags: drivers rad
    harness: ztest
    harness_config:
      fixture: rad_fixture
